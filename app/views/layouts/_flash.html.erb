<% flash.each do |status, msg| %>
	<%= content_tag(:div, class: "alert alert-#{status} alert-dismissable") do -%>
		<%= msg %>
		<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
	<% end -%>
<% end %>